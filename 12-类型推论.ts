let a: number;

a = 18;

// 类型推论写法
let b = 18;

const add = (num1: number, num2: number): number => {
  return num1 + num2;
};

// 类型推论写法：
const add_2 = (num1: number, num2: number) => {
  return num1 + num2;
};
